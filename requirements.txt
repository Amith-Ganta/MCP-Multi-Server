# Streamlit Community Cloud installs with pip, not uv, so it needs this file.
# Versions mirror pyproject.toml. The Windows-only manim server is NOT listed
# here because it cannot run on the Linux cloud (app.py skips it there).
fastmcp>=3.4.1
langchain>=1.3.4
langchain-mcp-adapters>=0.2.2
langchain-openai>=1.2.2
python-dotenv>=1.2.2
streamlit>=1.58.0
truststore>=0.10.4
